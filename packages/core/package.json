{
  "name": "@react-stateless-dialog/core",
  "repository": {
    "url": "https://github.com/boltcode-js/react-stateless-dialog",
    "directory": "packages/core",
    "type": "git"
  },
  "version": "0.4.0-beta2",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "types/index.d.ts",
  "react-native": "src/index.ts",
  "files": [
    "lib/**/*",
    "types/**/*",
    "src/**/*"
  ],
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "clean": "rm -rf lib types",
    "pub": "yarn clean && yarn build && yarn publish --access public",
    "compile": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  },
  "dependencies": {
    "zustand": "^4.4.1"
  },
  "devDependencies": {
    "@types/react-native": "^0.72.8",
    "typescript": "4.8.4"
  }
}
